linux:
  prehook:
    cmd: 'sudo apt-get'
    args: 'update -y'
  main:
    cmd: 'sudo apt-get'
    args: 'install openssh-server openssh-client -y'
mac:
  prehook:
    cmd: 'brew'
    args: 'update'
  main:
    cmd: 'brew'
    args: 'install openssh-client'
windows:
  main:
    cmd: 'echo'
    args: 'no support'
