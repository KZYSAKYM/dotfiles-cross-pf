linux:
  main:
    cmd: '[ -d ~/.tmux/plugins/tpm ] ||'
    args: 'git clone https://github.com/tmux-plugins/tpm $HOME/.tmux/plugins/tpm'
mac:
  main:
    cmd: '[ -d ~/.tmux/plugins/tpm ] ||'
    args: 'git clone https://github.com/tmux-plugins/tpm $HOME/.tmux/plugins/tpm'
windows:
  main:
    cmd: 'echo'
    args: 'No support for tmux'
